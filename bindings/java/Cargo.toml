[package]
name = "gatekeeper-java"
version = "0.4.5"
edition = "2024"
publish = false

[lib]
name = "gatekeeper_java"
crate-type = ["cdylib"]

[dependencies]
gatekeeper = { path = "../../core" }
jni = "0.21.1"
